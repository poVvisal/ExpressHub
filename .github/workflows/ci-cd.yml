# name: ExpressHub CI/CD Pipeline

# on:
#   push:
#     branches: [ main ]
#   pull_request:
#     branches: [ main ]

# env:
#   AWS_DEFAULT_REGION:   us-east-1
#   TF_DIR:               terraform/dev
#   DOCKER_IMAGE_NAME:    expresshub-app

# jobs:

#   # ─────────────────────────────────────────
#   # JOB 1: SonarQube Code Analysis
#   # ─────────────────────────────────────────
#   sonarqube:
#     name: 🔍 SonarQube Analysis
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4
#         with:
#           fetch-depth: 0  # Full history needed for SonarQube blame data

#       - name: SonarQube Scan
#         uses: sonarsource/sonarqube-scan-action@v5
#         env:
#           SONAR_TOKEN:    ${{ secrets.SONAR_TOKEN }}
#           SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
#         with:
#           args: >
#             -Dsonar.projectKey=ExpressHub
#             -Dsonar.projectName=ExpressHub
#             -Dsonar.sources=.

#       - name: SonarQube Quality Gate Check
#         uses: sonarsource/sonarqube-quality-gate-action@v1
#         timeout-minutes: 10
#         env:
#           SONAR_TOKEN:    ${{ secrets.SONAR_TOKEN }}
#           SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}

#   # ─────────────────────────────────────────
#   # JOB 2: Trivy Filesystem Security Scan
#   # ─────────────────────────────────────────
#   trivy-fs-scan:
#     name: 🛡️ Trivy Filesystem Scan
#     runs-on: ubuntu-latest
#     needs: sonarqube

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Trivy Filesystem Scan
#         uses: aquasecurity/trivy-action@0.31.0
#         with:
#           scan-type:    fs
#           scan-ref:     .
#           severity:     HIGH,CRITICAL
#           ignore-unfixed: true
#           exit-code:    1
#           format:       table

#   # ─────────────────────────────────────────
#   # JOB 3: Build Docker Image & Trivy Image Scan
#   # ─────────────────────────────────────────
#   build-and-scan:
#     name: 🐳 Build & Scan Docker Image
#     runs-on: ubuntu-latest
#     needs: trivy-fs-scan

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Log in to Docker Hub
#         uses: docker/login-action@v3
#         with:
#           username: ${{ secrets.DOCKER_USERNAME }}
#           password: ${{ secrets.DOCKER_PASSWORD }}

#       - name: Build Docker image (load locally for scanning)
#         uses: docker/build-push-action@v6
#         with:
#           context:   .
#           load:      true
#           tags:      ${{ secrets.DOCKER_USERNAME }}/${{ env.DOCKER_IMAGE_NAME }}:latest
#           cache-from: type=registry,ref=${{ secrets.DOCKER_USERNAME }}/${{ env.DOCKER_IMAGE_NAME }}:latest
#           cache-to:   type=inline

#       - name: Trivy Image Scan
#         uses: aquasecurity/trivy-action@0.31.0
#         with:
#           scan-type:      image
#           image-ref:      ${{ secrets.DOCKER_USERNAME }}/${{ env.DOCKER_IMAGE_NAME }}:latest
#           severity:       HIGH,CRITICAL
#           ignore-unfixed: true
#           exit-code:      1
#           format:         template
#           template:       "@/contrib/html.tpl"
#           output:         trivy-image-report.html

#       - name: Upload Trivy image scan report
#         uses: actions/upload-artifact@v4
#         if: always()
#         with:
#           name:            trivy-image-report
#           path:            trivy-image-report.html
#           retention-days:  14

#       - name: Push Docker image to Docker Hub
#         uses: docker/build-push-action@v6
#         with:
#           context: .
#           push:    true
#           tags: |
#             ${{ secrets.DOCKER_USERNAME }}/${{ env.DOCKER_IMAGE_NAME }}:latest
#             ${{ secrets.DOCKER_USERNAME }}/${{ env.DOCKER_IMAGE_NAME }}:${{ github.sha }}
#           cache-from: type=registry,ref=${{ secrets.DOCKER_USERNAME }}/${{ env.DOCKER_IMAGE_NAME }}:latest
#           cache-to:   type=inline

#   # ─────────────────────────────────────────
#   # JOB 4: Terraform — Provision EC2
#   # ─────────────────────────────────────────
#   terraform:
#     name: 🏗️ Terraform Provision
#     runs-on: ubuntu-latest
#     needs: build-and-scan
#     if: github.ref == 'refs/heads/main' && github.event_name == 'push'

#     outputs:
#       ec2_ip: ${{ steps.tf-output.outputs.ec2_ip }}

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region:            ${{ env.AWS_DEFAULT_REGION }}

#       - name: Setup Terraform
#         uses: hashicorp/setup-terraform@v3
#         with:
#           terraform_wrapper: false

#       - name: Restore Terraform state cache
#         uses: actions/cache@v4
#         with:
#           path:         ${{ env.TF_DIR }}/terraform.tfstate
#           key:          tfstate-${{ github.ref_name }}
#           restore-keys: tfstate-

#       - name: Terraform Init
#         working-directory: ${{ env.TF_DIR }}
#         run: terraform init

#       - name: Terraform Plan
#         id: tf-plan
#         working-directory: ${{ env.TF_DIR }}
#         env:
#           TF_VAR_grafana_password:           ${{ secrets.TF_VAR_GRAFANA_PASSWORD }}
#           TF_VAR_existing_security_group_id: ${{ secrets.TF_VAR_SECURITY_GROUP_ID }}
#           TF_VAR_existing_key_name:          ${{ secrets.TF_VAR_KEY_NAME }}
#         run: |
#           terraform plan -detailed-exitcode -out=tfplan
#           echo "exitcode=$?" >> $GITHUB_OUTPUT
#         continue-on-error: true

#       - name: Terraform Apply (only if changes detected)
#         if: steps.tf-plan.outputs.exitcode == '2'
#         working-directory: ${{ env.TF_DIR }}
#         env:
#           TF_VAR_grafana_password:           ${{ secrets.TF_VAR_GRAFANA_PASSWORD }}
#           TF_VAR_existing_security_group_id: ${{ secrets.TF_VAR_SECURITY_GROUP_ID }}
#           TF_VAR_existing_key_name:          ${{ secrets.TF_VAR_KEY_NAME }}
#         run: |
#           echo "🔧 Infrastructure changes detected — applying..."
#           terraform apply -auto-approve tfplan

#       - name: No changes detected
#         if: steps.tf-plan.outputs.exitcode == '0'
#         run: echo "✅ No infrastructure changes — skipping apply."

#       - name: Terraform plan failed
#         if: steps.tf-plan.outputs.exitcode != '0' && steps.tf-plan.outputs.exitcode != '2'
#         run: |
#           echo "❌ Terraform plan failed."
#           exit 1

#       - name: Get EC2 public IP
#         id: tf-output
#         working-directory: ${{ env.TF_DIR }}
#         run: echo "ec2_ip=$(terraform output -raw public_ip)" >> $GITHUB_OUTPUT

#       - name: Save Terraform state to cache
#         uses: actions/cache/save@v4
#         with:
#           path: ${{ env.TF_DIR }}/terraform.tfstate
#           key:  tfstate-${{ github.ref_name }}

#   # ─────────────────────────────────────────
#   # JOB 5: Deploy to EC2
#   # ─────────────────────────────────────────
#   deploy:
#     name: 🚀 Deploy to EC2
#     runs-on: ubuntu-latest
#     needs: terraform
#     if: github.ref == 'refs/heads/main' && github.event_name == 'push'

#     steps:
#       - name: Setup SSH key
#         run: |
#           mkdir -p ~/.ssh
#           echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/ec2-key.pem
#           chmod 600 ~/.ssh/ec2-key.pem

#       - name: Wait for EC2 & Docker to be ready
#         run: |
#           EC2_IP="${{ needs.terraform.outputs.ec2_ip }}"
#           echo "⏳ Waiting for Docker to be ready on EC2 (${EC2_IP})..."

#           for i in $(seq 1 20); do
#             echo "Attempt $i: checking Docker on EC2..."
#             if ssh -o StrictHostKeyChecking=no \
#                    -o ConnectTimeout=10 \
#                    -i ~/.ssh/ec2-key.pem \
#                    ubuntu@${EC2_IP} \
#                    "command -v docker > /dev/null 2>&1 && sudo docker info > /dev/null 2>&1"; then
#               echo "✅ Docker is ready on EC2."
#               break
#             fi
#             if [ $i -eq 20 ]; then
#               echo "❌ Docker never became ready after 20 attempts. Aborting."
#               exit 1
#             fi
#             echo "⏳ Not ready yet — retrying in 15 seconds..."
#             sleep 15
#           done

#       - name: Zero-downtime container swap on EC2
#         run: |
#           EC2_IP="${{ needs.terraform.outputs.ec2_ip }}"
#           DOCKER_USER=$(echo "${{ secrets.DOCKER_USERNAME }}" | tr '[:upper:]' '[:lower:]')
#           SSH="ssh -o StrictHostKeyChecking=no -i ~/.ssh/ec2-key.pem ubuntu@${EC2_IP}"

#           # Pull new image
#           $SSH "sudo docker pull ${DOCKER_USER}/${{ env.DOCKER_IMAGE_NAME }}:latest"

#           # Start new container on temp port 5001
#           $SSH "sudo docker run -d --name foodexpress-js-new -p 5001:5000 ${DOCKER_USER}/${{ env.DOCKER_IMAGE_NAME }}:latest"

#           # Stop and remove old container
#           $SSH "sudo docker stop foodexpress-js || true"
#           $SSH "sudo docker rm foodexpress-js || true"

#           # Re-run new container on production port 5000
#           $SSH "sudo docker stop foodexpress-js-new"
#           $SSH "sudo docker rm foodexpress-js-new"
#           $SSH "sudo docker run -d --name foodexpress-js -p 5000:5000 ${DOCKER_USER}/${{ env.DOCKER_IMAGE_NAME }}:latest"

#           echo "✅ Container deployed successfully on ${EC2_IP}:5000"

#       - name: Backup Terraform state to EC2
#         run: |
#           EC2_IP="${{ needs.terraform.outputs.ec2_ip }}"
#           if [ -f "${{ env.TF_DIR }}/terraform.tfstate" ]; then
#             scp -o StrictHostKeyChecking=no \
#                 -i ~/.ssh/ec2-key.pem \
#                 ${{ env.TF_DIR }}/terraform.tfstate \
#                 ubuntu@${EC2_IP}:~/terraform.tfstate.backup
#             echo "📦 Terraform state backed up to EC2."
#           fi

#       - name: Print access URLs
#         run: |
#           EC2_IP="${{ needs.terraform.outputs.ec2_ip }}"
#           echo ""
#           echo "════════════════════════════════════"
#           echo "  ✅ Deployment Complete!"
#           echo "════════════════════════════════════"
#           echo "  🌐 App:      http://${EC2_IP}:5000"
#           echo "  📊 Grafana:  http://${EC2_IP}:3000"
#           echo "  📈 Prometheus: http://${EC2_IP}:9090"
#           echo "════════════════════════════════════"